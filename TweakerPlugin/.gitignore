# CMake / build
/build/
/out/
# FetchContent clones (LunaSVG + its plutovg submodule), see cmake/LunaSVG.cmake - deliberately
# outside /build/ so every build tree shares one clone.
/.deps/
CMakeUserPresets.json
compile_commands.json

# Vendored SDKs: proprietary/copyrighted, not redistributable - fetch
# locally per cmake/Quest3DSDK.cmake and cmake/DirectXSDK.cmake.
/Quest3D-SDK/
/DirectX-SDK-June2010/
/ImGui/
/Detours/

# Service
/Logs/